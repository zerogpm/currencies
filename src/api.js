import axios from "axios";

const searchImages = async (term) => {
  //const response = await axios.get(`https://swapi.py4e.com/api/people/${term}/`);
  //return response.data;
};

export default searchImages;
